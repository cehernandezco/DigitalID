<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.digitalid">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DigitalID">
        <activity
            android:name=".activities.HomeMainActivity"
            android:label="@string/title_activity_home_main"></activity>
        <activity
            android:name=".activities.AddUserScrollingActivity"
            android:label="@string/title_activity_add_user_scrolling"
            android:theme="@style/Theme.DigitalID.NoActionBar" />
        <activity android:name=".activities.SignUpActivity" />
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>